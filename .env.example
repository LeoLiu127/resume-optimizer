# =========================================================
# 简历优化大师 - 前端环境变量示例
#
# 安全 / CORS 提示：
#  - 后端代理 MiniMax API Key，不再把 Key 暴露到前端 bundle。
#    任何 VITE_MINIMAX_* 变量已被移除，请到 server/.env 配置 MINIMAX_API_KEY。
#  - 前端只需配置后端 API 的访问地址。
#  - 如果遇到 /api 请求跨域问题，请用 Vite devServer.proxy 或 Nginx 反向代理
#    把 /api 与后端同源暴露（详见 DEPLOY.md）。
# =========================================================

# 后端 API 地址（默认与前端同主机:4000）。
# 本地开发：保持 http://localhost:4000
# 阿里云部署：改成 https://你的域名/api 并让 Vite/Nginx 转发
VITE_API_BASE=http://localhost:4000
